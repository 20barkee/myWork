package interfaces;

public class formLetter {
	
int pos=line.indexOf(sub);
while(pose>=-) {
	line=line.substring(0,pos)+repl+line.substring(pos+sub.length())
pos=line.indexOf(sub)
}
return line;


public void createPersonalized() {
	for(Customer temp: customers) {
		for(String temp: lines) {
			this.replaceAll(temp,"@", temp.setName());
			this.replaceAll(temp, "&", temp.setCity());
			this.replaceAll(temp, "$", temp.setState();
		}
	}
	for(String temp:lines) {
		System.out.println(temp);
	}
}
}
